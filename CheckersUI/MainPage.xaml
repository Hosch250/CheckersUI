<Page
    x:Class="CheckersUI.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CheckersUI"
    xmlns:controls="using:CheckersUI.CustomControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Margin="50, 0">
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width=".75*" />
            <ColumnDefinition Width="2*" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <TextBlock Text="{Binding Status}"
                   HorizontalAlignment="Center"
                   Margin="0,10"
                   FontWeight="Black"
                   FontSize="20"
                   Grid.Column="1" />
        <Border MaxHeight="641"
                MaxWidth="641"
                BorderThickness="1"
                BorderBrush="Black"
                Grid.Row="1"
                VerticalAlignment="Top"
                Grid.Column="1">
            <controls:EightPieceBoard Orientation="White"
                                      Board="{Binding Controller.Board, Mode=OneWay}"
                                      Selection="{Binding Selection, Mode=TwoWay}" />
        </Border>
        <Grid Grid.Column="2"
              Grid.Row="1"
              VerticalAlignment="Bottom"
              Height="75"
              Margin="0,0,0,100">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Button Background="Transparent"
                        Command="{Binding DisplaySettingsCommand}">
                    <Image Source="../Assets/gear.png" />
                </Button>
                <Button Background="Transparent"
                        Grid.Column="1"
                        Command="{Binding NewGameCommand}">
                    <Image Source="../Assets/plus.png" />
                </Button>
            </Grid>
        </Grid>
        <Grid Grid.RowSpan="3"
              Grid.ColumnSpan="3"
              VerticalAlignment="Center"
              HorizontalAlignment="Center"
              Width="500"
              Background="MintCream"
              BorderBrush="Black"
              BorderThickness="2"
              Padding="20"
              Visibility="{Binding DisplaySettingsGrid}">
            <StackPanel>
                <StackPanel>
                    <TextBlock FontWeight="Medium">Theme:</TextBlock>
                    <ComboBox ItemsSource="{Binding Themes, Mode=OneTime}"
                              SelectedItem="{Binding SelectedTheme, Mode=TwoWay}" />
                </StackPanel>
                <StackPanel>
                    <TextBlock FontWeight="Medium">Sound Effects:</TextBlock>
                    <ToggleSwitch IsOn="{Binding EnableSoundEffects, Mode=TwoWay}" />
                </StackPanel>
            </StackPanel>
            <Button VerticalAlignment="Bottom"
                    HorizontalAlignment="Right"
                    Margin="5"
                    Command="{Binding HideSettingsCommand}">
                Close
            </Button>
        </Grid>
    </Grid>
</Page>
